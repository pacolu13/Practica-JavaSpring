server.port=9525
spring.application.name=springboot-aplicaciones

# Configuración de la base de datos PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:3306/db_libreria
spring.datasource.username=springuser
spring.datasource.password=spring123

# Configuración de JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# generar el esquema de la base de datos automáticamente
spring.jpa.generate-ddl=true 
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Configuración de seguridad
spring.security.user.name=admin
spring.security.user.password=admin123